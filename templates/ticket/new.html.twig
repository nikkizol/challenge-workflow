{% extends 'base.html.twig' %}

{% block title %}New Ticket{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create new Ticket</h1>

        {{ include('ticket/_form.html.twig') }}

        <a class="btn btn-outline-primary mt-2" href="{{ path('ticket_index') }}">back to list</a>
    </div>
{% endblock %}
